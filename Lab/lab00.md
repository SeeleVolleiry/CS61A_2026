# Organizing files

## terminal commands(终端命令)
ls : list all files in current directory.

cd <path to directory> : change directory.
    cd ~ : change to home directory.

mkdir <directory name> : make a new directory.

## CLI & GUI 
CLI: command line interface, such as terminal
GUI: graphical user interface, such as 资源管理器.

## Python

python3 lab00.py
python3 -i lab00.py
python3 -m doctest lab00.py -v : v stands for verbose

python3 ok --score
python3 ok -q