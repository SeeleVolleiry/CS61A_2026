# Homework05

## Scheme Readings

CS61A Scheme Specification：https://website.cs61a.org/articles/scheme-spec/
Scheme Built-In Procedure Reference：https://website.cs61a.org/articles/scheme-builtins/

## Q1：Add Links

Write a function add_link that takes in two linked lists, link1 and link2, and returns a new linked list that concatenates link2 to the end of link1.

递归结构第一想法就是使用递归。

## Q2：Mutable Mapping 递归

Implement deep_map_mut(func, s), which applies the function func to each element in the linked list s. If an element is itself a linked list, recursively apply func to its elements as well.

Your implementation should mutate the original linked list. Do not create any new linked lists. The function returns None.

Hint: You can use the built-in isinstance function to determine if an element is a linked list.
```python
>>> s = Link(1, Link(2, Link(3, Link(4))))
>>> isinstance(s, Link)
True
>>> isinstance(s, int)
False
```

Construct Check: The final test case for this problem checks that your function does not create any new linked lists. If you are failing this doctest, make sure that you are not creating link lists by calling the constructor, i.e.

s = Link(1)

## Q3：Prime

Write a function that returns whether a number is prime or not in O(sqrt(n)) time, where sqrt means square root. You can assume n >= 2.

Hint: 
    you don't need to check whether every single number that is smaller than n divides n
    要 O(√n) 时间判断质数。只需检查 2 到 √n（含）之间是否有能整除 n 的数，若无则 n 为质数。

## Scheme的代码在hw05.scm文件中

## Q4：Pow

Implement a procedure pow that raises a number base to the power of a nonnegative integer exp.
The number of recursive pow calls should grow logarithmically with respect to exp, rather than linearly.
For example, (pow 2 32) should result in 5 recursive pow calls rather than 32 recursive pow calls.
## Q5：Repeatedly Cube

mplement repeatedly-cube, which receives a number x and cubes it n times.

Here are some examples of how repeatedly-cube should behave:
```scheme
scm> (repeatedly-cube 100 1) ; 1 cubed 100 times is still 1
1
scm> (repeatedly-cube 2 2) ; (2^3)^3
512
scm> (repeatedly-cube 3 2) ; ((2^3)^3)^3
134217728
```

## Q6：Cadr and Caddr

`pair`: first as the car and rest as the cdr

Define the procedure `cadr`, which returns the second element of a list.
Also define `caddr`, which returns the third element of a list.
Try writing cadr and caddr in terms of car and cdr.